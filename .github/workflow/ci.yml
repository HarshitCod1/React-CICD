name: React Native Ci pipeline

on:
 push:
    branches:
      -main

jobs: 
    build-and-test:
        runs-on: ubuntu-latest
        steps:
          - name: "Setup Node js"
            uses: actions/setup-node@v4
            with:
              node-version: "20"